<header class="header">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand text-uppercase" href="{{ site.baseurl }}/">{{ site.author }}</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-header">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbar-header">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item {{ "active" if page == "collections" }}">
            <a class="nav-link" href="#">Collections</a>
          </li>
          <li class="nav-item {{ "active" if page == "photos" }}">
            <a class="nav-link" href="{{ site.baseurl }}/photos.html">Photos</a>
          </li>
          <li class="nav-item {{ "active" if page == "videos" }}">
            <a class="nav-link" href="{{ site.baseurl }}/videos.html">Vidéos</a>
          </li>
          <li class="nav-item {{ "active" if page == "editorial" }}">
            <a class="nav-link" href="#">Éditorial</a>
          </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
          <input class="form-control mr-sm-2" type="search" placeholder="Recherche par mot-clé">
          <button class="btn btn-outline-light my-2 my-sm-0" type="submit"><i class="fas fa-search"></i></button>
        </form>
      </div>
    </div>
  </nav>
</header>
