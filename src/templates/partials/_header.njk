<header class="header">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand text-uppercase" href="{{ site.baseurl }}/">{{ site.author }}</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-header">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbar-header">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item {{ "active" if page == "photos" }}">
            <a class="nav-link" href="{{ site.baseurl }}/photos.html">Photos</a>
          </li>
          <li class="nav-item {{ "active" if page == "books" }}">
            <a class="nav-link" href="#">Livres</a>
          </li>
          <li class="nav-item {{ "active" if page == "fine-art" }}">
            <a class="nav-link" href="#">Fine Art</a>
          </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
          <div class="input-group">
            <input class="form-control" type="search" placeholder="Recherche par mot-clé">
            <div class="input-group-append">
              <button class="btn btn-outline-light" type="submit"><i class="fas fa-search"></i></button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </nav>
</header>
